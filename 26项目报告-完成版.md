# 《高级软件工程》软件项目分析报告

学院：待填写
学系：待填写
专业：待填写
课程名称：高级软件工程
学生姓名（学号）：待填写
授课教师：孙玉霞
日期：2026年6月12日

## 项目的 GitHub 链接地址

原始项目地址：https://github.com/nsidnev/fastapi-realworld-example-app

本课程项目仓库地址：待填写。当前本地仓库的 origin 仍指向原始开源项目，因此提交前需要替换为本人课程项目仓库地址。

链接内容包括：1. 代码，包含 FastAPI 后端项目、数据库迁移、自动化测试和静态演示前端；2. 数据集，本项目不依赖外部训练数据集，主要使用文章、用户、评论、审核记录等业务数据以及测试夹具和演示数据；3. 用法 README 文件，说明本地数据库、后端服务、AI 服务、静态演示页面和测试命令的运行方式。

## 项目功能介绍

本项目的基础来源是 FastAPI RealWorld Example App。原始项目是一个按照 RealWorld/Conduit 思路实现的后端示例，核心目标不是做一个完整的商业产品，而是用接近真实业务的文章社区场景展示 FastAPI 后端工程的组织方式。原项目主要提供用户注册、用户登录、当前用户信息维护、文章创建、文章查询、文章更新、文章删除、资料信息查询、标签读取、评论读写等基础能力。它通过 JWT 完成身份识别，通过 Pydantic 对请求和响应进行结构校验，通过异步数据库访问层保存用户、文章、标签和评论等数据。

从用户角度看，原始项目解决的是一个文章社区后端最基本的内容发布问题。用户可以注册账号并登录系统，在认证通过后发布自己的文章；文章由标题、描述、正文和标签组成，系统会根据标题生成用于访问的文章标识。用户可以按条件查看文章列表，也可以查看单篇文章详情。作者可以修改或删除自己的文章，非作者无法直接修改他人内容。这部分功能体现了后端系统中常见的认证、权限、参数校验和资源归属控制。

从数据和工程角度看，原始项目的价值在于它提供了一套清晰的后端分层。路由层负责 HTTP 请求处理和依赖注入，数据模型层负责输入输出格式，领域模型表达核心业务对象，仓储层封装数据库读写，服务层处理不直接属于 CRUD 的逻辑。项目还包含数据库迁移、Docker 运行配置、Postman Collection、pytest 测试和代码风格配置。因此，它适合作为课程项目的基础工程，用来分析一个后端系统如何从请求处理、数据、测试和部署几个方面组织起来。

需要说明的是，本文在介绍原始项目功能时只讨论它作为文章社区后端示例提供的基础能力，不把本课程后续加入的 AI 写作、AI 审核、文章库问答、语义推荐、后台治理和静态演示前端算入原项目功能。课程项目的重点是：在不破坏原有文章发布后端结构的前提下，把 AI 能力接入到内容创作、内容发现和内容治理流程中，使原本偏基础的后端示例变成一个具有智能辅助能力的文章平台。

## 本项目所参考或基于的论文、项目来源

本项目主要参考并基于开源项目 nsidnev/fastapi-realworld-example-app。该项目围绕 RealWorld/Conduit 文章社区规范构建，目的是展示 FastAPI 在真实后端项目中的使用方式，包括类型注解、依赖注入、异步数据库访问、JWT 鉴权、数据模型、仓储模式、自动化测试和容器化运行。课程项目并没有复现某一篇具体论文，而是以这个开源后端工程为基础进行功能分析和二次扩展。

项目来源可以分为三类。第一类是 Web 后端工程来源，包括 FastAPI、Pydantic、PostgreSQL、Alembic、pytest 和 Docker 等技术栈，它们构成了项目的基础开发和验证环境。第二类是文章社区业务来源，即 RealWorld/Conduit 规范所代表的用户、文章、标签和评论等典型内容平台对象。第三类是 AI 增强功能来源，包括 OpenAI-compatible 大模型调用方式、本地 vLLM 推理服务、Qwen3-8B 语言模型、sentence-transformers 向量模型以及 pgvector 语义检索能力。

在 AI 功能设计上，本项目参考了当前常见的智能内容系统架构：写作助手用于辅助创作者生成反馈和改写建议，内容审核模型用于发现潜在风险，语义向量检索用于推荐和问答，RAG 流程用于让模型回答受限于已有文章库。项目没有把大模型当作孤立聊天窗口，而是把它嵌入原有业务流程，使 AI 输出能够被后端数据结构、权限控制、审核状态和前端交互承接。

## 本项目在参考项目基础上做了哪些改动

本课程项目的改进重点不是简单增加若干技术入口，而是围绕文章平台的不同角色补充完整功能。整体上，系统从一个基础文章后端扩展为“创作辅助、文章阅读、智能问答、内容推荐、AI 初审、人工复核、后台管理”结合的应用。改进后的系统既服务普通用户，也服务内容创作者和审核管理者。

在普通用户模块，系统增加了更完整的前端交互体验。用户可以在演示页面中浏览文章、进入阅读页、查看评论、查看相关推荐，并通过文章库小助手提出自然语言问题。文章库小助手隐藏在顶部用户区域旁边，以一个图标提示入口，点击后从右侧弹出侧边栏。用户输入问题后，系统会从文章库中查找相关内容，返回回答和来源文章；用户选择某篇来源文章后，可以跳转到对应阅读位置。这使原本只能按列表浏览的文章系统具备了基于语义的查找和问答能力。

在创作者模块，系统加入了写作辅助流程。创作者在发布前可以对草稿进行分析，获得摘要、推荐标签、阅读时间、内容评分、风险提示和修改建议。每条建议不只是静态文字，用户可以选择某条建议进行针对性改写，系统会给出改写后的正文、修改理由和前后差异。创作者还可以使用 AI 润色功能，输入“更简洁”“更流畅”“增强逻辑”等指令，让系统对正文进行润色，并展示原文与润色结果的差异。写作页还保留草稿版本，方便用户在改写和润色之间回退或比较。

在阅读和内容发现模块，系统增加了语义推荐和文章库问答能力。系统会根据文章标题、摘要、标签和正文理解文章主题，为当前文章推荐语义接近的其他文章，并给出推荐原因。对于用户提出的开放问题，系统先检索候选文章，再对候选内容进行排序，降低测试文章、占位文章等无关内容的排名，最后基于检索到的文章生成回答。这样用户不必只依赖精确关键词，也能通过“有没有关于城市生活和注意力恢复的文章”这类自然语言找到相关内容。

在评论和互动模块，系统加入了评论点赞、评论举报和评论 AI 初审能力。用户提交评论时，系统可以先判断评论是否存在广告、攻击性语言或其他风险。如果评论风险较低，可以正常显示；如果存在明显风险，则进入待审核状态，等待管理员复核。用户也可以对不合适的评论或文章进行举报，为后续人工处理提供入口。

在审核管理者模块，系统补充了后台治理功能。审核管理者可以查看平台概览、文章管理、评论管理、举报记录、待审核内容和审核结果。对于 AI 标记为风险的文章或评论，管理员可以进行人工判断，选择通过或拒绝，并留下处理记录。系统会保存审核状态变化、处理原因和通知信息，使内容治理不只是简单拦截，而是形成“AI 初筛、人工复核、结果通知、记录留存”的闭环。

在工程和运行层面，项目增加了与这些功能配套的数据持久化、配置项、测试和演示页面。AI 结果、审核记录、推荐依据、内容状态和通知都能在系统中保留，避免只停留在一次性模型输出。演示前端也围绕实际使用流程进行了优化，例如写作页中左右两侧保持等高，草稿分析和草稿版本不会挤压 AI 润色区域，文章库小助手从右侧抽屉打开，整体交互更接近真实应用。

## 实验结果

实验结果主要围绕上述功能进行验证，而不是只罗列代码改动。第一类验证是普通用户和阅读场景。文章列表、文章阅读、评论展示、相关推荐和文章库小助手可以在静态演示前端中完成闭环操作。用户能够提出自然语言问题，系统返回文章库回答和来源文章；当用户点击来源文章时，可以跳转到阅读页进行查看。

【图1 普通用户文章浏览与阅读页效果图位置】

【图2 文章库小助手侧边栏问答与来源文章跳转效果图位置】

第二类验证是创作者写作场景。草稿分析能够返回摘要、标签、评分和建议，建议列表在草稿分析面板内部滚动，不会造成页面双重滚动。用户选择建议后能够得到针对性改写结果，并可以查看改写差异。AI 润色区域可以根据用户输入的润色指令生成新版本，并展示原文和润色结果。草稿版本列表能够保存不同阶段内容，方便用户比较。

【图3 创作者写作页：草稿分析、建议改写、草稿版本效果图位置】

【图4 AI 润色前后对比效果图位置】

第三类验证是审核管理者场景。风险文章和风险评论不会直接进入公开展示，而是进入待审核状态。审核管理者可以在后台查看待处理内容、审核原因、风险类型和处理记录，并对内容执行通过或拒绝。对于用户举报内容，系统也能进入后台处理流程，保证平台治理有记录可查。

【图5 审核管理端：待审核文章、待审核评论和处理记录效果图位置】

第四类验证是自动化测试结果。AI 服务层相关测试共 28 个用例全部通过，覆盖文章分析、评论审核、建议改写、文章库问答、无关测试文章降权、语义向量离线回退、润色迭代和推荐重排等能力。API 和治理相关测试中，67 个用例通过，覆盖管理员概览、文章管理、评论管理、举报处理、用户内容列表、AI 审核、推荐、文章库问答、文章创建更新删除、搜索、润色和建议改写等流程。

当前仍有 1 个测试环境层面的资源告警：在某个文章重复标题测试的准备阶段，pytest 将一个未关闭的本地模型服务 socket 告警提升为错误。该问题不是业务功能断言失败，而是外部 AI 客户端连接清理不够彻底。后续可以通过统一关闭模型客户端、加强测试替身隔离或在测试生命周期中显式释放连接来解决。

前端验证方面，静态页面可以正常访问，JavaScript 语法检查通过。写作页近期重点修复了两类视觉问题：一是草稿分析外框不再出现双重滚动，只保留建议列表内部滚动；二是草稿分析和草稿版本与左侧正文编辑区域保持一致高度，不再把下方 AI 润色区域挤开。整体演示流程已经能够较完整地展示普通用户、创作者和审核管理者三个角色的核心功能。

## 项目总结

通过本次课程项目，我对一个 FastAPI 后端示例项目进行了结构分析，并在其基础上完成了面向真实业务场景的 AI 功能扩展。原项目最有价值的地方不是界面效果，而是提供了清晰的后端分层和测试基础。理解这个项目时，需要先弄清楚用户、文章、评论、标签等基础对象之间的关系，再判断新增功能应该放在什么业务位置。如果只关注某个模型调用，很容易把 AI 功能做成孤立工具；而本项目更重要的是把 AI 放进内容生产、内容发现和内容治理流程中。

本项目的主要难点在于平衡 AI 能力和传统后端工程的稳定性。大模型输出具有不确定性，语义检索也可能召回无关内容，因此不能简单相信模型结果。项目通过结构化输出校验、语义召回后的再次排序、审核状态流转、人工复核和测试替身等方式降低风险。尤其是在文章库问答场景中，早期出现过测试文章相似度过高的问题，后续通过对问题关键词、文章标题、标签、摘要和正文进行综合排序，并对明显测试内容降权，使结果更符合用户直觉。

从软件工程工具角度看，本项目使用 Git 管理代码，使用数据库迁移维护数据结构，使用 pytest 做回归测试，使用 FastAPI 和 Pydantic 保证输入输出数据契约，使用 Docker 和本地数据库支持运行环境，使用静态演示页面验证用户流程。这些工具共同说明，一个课程项目不只是能跑出模型结果，还需要有可复现的运行方式、可验证的测试结果、可解释的功能边界和可维护的工程结构。

从软件开发与 AI 结合的角度看，本项目体现了三种结合方式：第一，AI 辅助创作者，提高文章草稿质量；第二，AI 辅助读者，提高文章发现效率；第三，AI 辅助审核管理者，提高内容治理效率。但 AI 不是最终裁判，尤其是审核场景仍需要人工复核。这样的设计更符合真实系统中“模型辅助、人来确认、过程留痕”的原则。

未来工作可以继续从四个方向完善。第一，优化文章库问答和推荐质量，引入更稳定的混合检索、文章分块和引用片段展示。第二，完善审核管理端，增加更细的状态流转、申诉机制和统计报表。第三，解决当前测试中外部模型连接释放的问题，让自动化测试结果完全稳定。第四，把演示前端进一步工程化，补充端到端测试和更多效果截图，使项目展示更加完整。
