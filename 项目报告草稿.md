# 《高级软件工程》软件项目分析报告草稿

## 项目的 GitHub 链接地址

原始项目地址：https://github.com/nsidnev/fastapi-realworld-example-app

本课程项目仓库地址：待填写为自己的 GitHub 仓库地址。

仓库内容包括：

1. 代码：FastAPI 后端应用、数据库访问层、服务层、接口路由、测试用例。
2. 数据集：本项目不依赖外部训练数据集，主要使用 RealWorld/Conduit API 规范中的文章、用户、标签、评论等业务数据模型；测试数据由 pytest fixture 和 Postman Collection 构造。
3. 用法 README 文件：原项目提供 README.rst，说明环境变量、数据库、测试、Docker 部署和项目目录结构。本课程项目新增 AI 辅助文章分析接口，可在 Swagger 文档或测试用例中查看使用方式。

## 项目功能介绍

本项目基于 FastAPI RealWorld Example App，是一个实现 RealWorld/Conduit 社区文章系统后端接口的完整示例。项目采用 FastAPI 作为 Web 框架，使用 Pydantic 进行请求和响应模型校验，使用 asyncpg、databases、aiosql 和 PostgreSQL 完成异步数据库访问，并通过 Alembic 管理数据库迁移。系统核心业务围绕文章发布平台展开，覆盖用户注册、登录、JWT 鉴权、用户资料维护、关注关系、文章创建、文章查询、文章更新、文章删除、文章收藏、个性化信息流、标签查询和评论管理等功能。

在用户模块中，系统支持新用户注册、登录认证和当前用户信息更新。用户密码不会以明文形式保存，而是通过安全服务进行加盐哈希处理；登录成功后系统返回 JWT，后续需要身份验证的接口通过 Authorization 请求头完成用户识别。在资料和关注模块中，用户可以查看其他用户的公开资料，也可以关注或取消关注其他作者。该关系会影响个人信息流接口，使用户能够优先看到自己关注作者发布的文章。

文章模块是本项目最核心的业务模块。用户可以创建文章，系统会根据标题生成 slug，避免文章 URL 标识重复；文章可以携带标签列表，标签不存在时由仓储层自动创建。文章查询接口支持按照标签、作者、被某用户收藏等条件过滤，并支持 limit 和 offset 分页。文章详情接口返回标题、摘要、正文、标签、作者信息、收藏状态、收藏数、创建时间和更新时间。作者本人可以更新或删除自己的文章，非作者访问修改接口时会被权限控制拦截。收藏功能允许登录用户将文章加入或移出收藏列表，系统会同步维护收藏状态和收藏数量。评论模块支持在指定文章下创建、查看和删除评论，只有评论作者拥有删除权限。

从软件工程角度看，项目结构比较清晰：api 目录负责 Web 路由、依赖注入和错误处理；services 目录放置不直接依赖数据库的业务逻辑；db/repositories 目录封装数据访问；models/domain 和 models/schemas 分别表达领域对象和接口输入输出模型；tests 目录覆盖 API、服务和数据库查询。项目还包含 Dockerfile、docker-compose.yml、Postman Collection、pytest 覆盖率配置和代码风格工具，体现了后端项目从开发、测试到部署的一套工程化流程。

本课程项目在原有文章平台基础上增加了 AI 辅助文章分析能力。新增接口允许登录用户在正式发布文章前提交文章草稿，系统返回自动摘要、推荐标签、阅读时长估计、内容评分和改进建议。该功能把 AI 辅助写作与原有文章业务结合起来，使系统不仅能保存和分发内容，还能在内容生产阶段提供质量反馈。为了保证课程项目可复现，当前实现采用本地启发式 NLP 规则模拟 AI 分析流程，不依赖外部大模型 API Key；后续可以把服务层替换为 OpenAI、通义千问、智谱等大模型接口，而不需要改变外部 API 契约。

## 本项目所参考/基于的论文或项目来源

本项目主要参考开源项目 nsidnev/fastapi-realworld-example-app。该项目实现了 Gothinkster RealWorld/Conduit API 规范，目标是用真实后端项目展示 FastAPI、异步数据库访问、JWT 认证、Pydantic 数据模型、仓储模式和自动化测试等技术。课程项目没有基于特定论文，而是基于开源工程实践进行分析和二次开发。

相关技术来源包括：

1. FastAPI 官方设计思想：通过类型注解和 Pydantic 模型完成接口校验、文档生成和依赖注入。
2. RealWorld/Conduit API 规范：提供文章社区系统的标准业务接口和数据结构。
3. pytest 自动化测试实践：通过 fixture、异步测试和覆盖率门槛保障功能质量。
4. AI 辅助软件工程思想：将智能分析能力嵌入传统 Web 后端，使系统在内容生产环节提供自动化建议。

## 本项目在参考项目基础上做了哪些改动

本课程项目新增了一个 AI 辅助文章分析功能，主要改动如下：

1. 新增服务层文件 `app/services/article_ai.py`。该文件实现 `analyze_article_content` 函数，对文章标题、描述和正文进行文本分析，生成自动摘要、推荐标签、阅读时间、内容评分和写作建议。实现中包含停用词过滤、关键词统计、标签归一化、摘要截断和评分规则。
2. 扩展文章 Schema：在 `app/models/schemas/articles.py` 中新增 `ArticleAIAnalysis` 和 `ArticleAIAnalysisInResponse`，保持和原项目相同的 camelCase API 输出风格，例如 `recommendedTagList`、`readingTimeMinutes`、`contentScore`。
3. 新增文章分析接口：在 `app/api/routes/articles/articles_resource.py` 中增加 `POST /api/articles/ai/analyze` 路由。接口要求用户登录，接收和创建文章一致的草稿结构，但不会写入数据库，而是返回分析结果，适合发布前预览。
4. 补充接口测试：在 `tests/test_api/test_routes/test_articles.py` 中增加登录用户调用 AI 分析接口的测试，验证 HTTP 状态码、摘要、推荐标签和内容评分。
5. 补充服务测试：新增 `tests/test_services/test_article_ai.py`，覆盖正常草稿、短草稿和长正文摘要截断等场景，验证服务层算法的主要分支。

本次改动没有修改数据库表结构，也没有改变原有文章发布、查询、收藏和评论接口，因此对原系统影响较小。新增功能被封装在独立服务层中，后续如果要接入真实大模型，只需要把 `article_ai.py` 中的分析逻辑替换为模型调用，路由层和响应 Schema 可以保持稳定。这种设计体现了接口契约稳定、服务层可替换、测试先行和低耦合扩展的工程思想。

## 实验结果

本次实验完成了功能实现、语法验证、服务层手动验证和测试用例补充。

新增接口示例：

```http
POST /api/articles/ai/analyze
Authorization: Token <jwt>
Content-Type: application/json

{
  "article": {
    "title": "FastAPI article quality review",
    "description": "AI assisted review for FastAPI article drafts.",
    "body": "FastAPI testing quality coverage ...",
    "tagList": ["AI", "FastAPI"]
  }
}
```

服务层验证输出示例：

```json
{
  "summary": "AI assisted review for FastAPI article drafts.",
  "recommendedTagList": ["ai", "fastapi", "quality", "testing", "coverage"],
  "readingTimeMinutes": 2,
  "contentScore": 100,
  "suggestions": ["The draft is ready to publish after human review."]
}
```

已执行的验证命令：

```bash
python -m compileall app/services/article_ai.py app/models/schemas/articles.py app/api/routes/articles/articles_resource.py tests/test_services/test_article_ai.py tests/test_api/test_routes/test_articles.py
```

执行结果：新增和修改的 Python 文件均编译通过，未发现语法错误。

同时执行了服务层直接调用，验证 `analyze_article_content` 可以返回符合接口 Schema 的 JSON 结果。当前本地环境没有安装项目完整测试依赖，执行 `pytest tests/test_services/test_article_ai.py` 时在加载 `tests/conftest.py` 阶段失败，错误为 `ModuleNotFoundError: No module named 'asgi_lifespan'`；此外当前环境没有 `poetry` 命令，无法直接按原 README 的方式安装依赖。因此完整自动化测试需要在安装项目依赖后执行：

```bash
poetry install
pytest
```

或在已有 Python 虚拟环境中安装 `pyproject.toml` 中列出的运行和测试依赖后执行 pytest。由于本项目原配置要求 100% 覆盖率，新增服务测试和接口测试是必要的，否则新增代码会导致覆盖率门槛失败。

## 项目总结

通过本次课程项目，我对一个较完整的 FastAPI 后端工程进行了结构分析和功能扩展。原项目虽然是示例项目，但已经包含路由层、依赖注入、领域模型、接口 Schema、仓储层、服务层、数据库迁移和自动化测试等真实工程常见组成部分。阅读代码时需要先理解业务边界，再判断功能应该放在哪一层。如果把 AI 分析逻辑直接写在路由函数中，短期实现会很快，但会导致接口层承担过多职责，后续也难以测试和替换。因此本次改动把分析逻辑封装到 `app/services/article_ai.py`，路由层只负责鉴权、接收请求和返回响应，Schema 层负责定义稳定的数据契约。

本项目的主要难点不是算法复杂度，而是如何在不破坏原有系统结构的前提下加入新能力。RealWorld 项目已有明确的文章创建、标签和认证流程，新增 AI 功能必须和这些流程自然结合。最终选择“发布前文章草稿分析”作为切入点，是因为它不需要新增数据库表，也不会影响已有文章持久化逻辑，却能体现 AI 对软件系统的增强作用。当前实现使用本地规则模拟 AI：通过关键词统计推荐标签，根据描述生成摘要，按词数估计阅读时间，并按照标题、摘要、正文长度和标签完整度计算内容评分。虽然它还不是大语言模型，但接口设计已经为未来接入真实模型预留了位置。

在软件工程工具方面，本项目使用 Git 管理改动，使用 pytest 组织测试，使用 Pydantic 保证输入输出结构，使用 FastAPI 自动生成接口文档，使用 Docker 和 Alembic 支持部署与数据库迁移。实验过程中也暴露了环境复现的重要性：当前机器缺少 `poetry` 和 `asgi_lifespan`，导致不能直接运行完整测试；这说明课程项目提交时应同时提供清晰的依赖安装说明，并尽量在干净环境中验证一次。未来工作可以从三个方向继续改进：第一，接入真实大模型 API，让摘要和建议更接近自然语言写作助手；第二，把用户反馈保存下来，用于优化推荐标签和评分规则；第三，增加限流、缓存和审计日志，防止 AI 接口被滥用，并提升生产环境可靠性。
